import { GetServerSideProps } from "next";
import React from "react";

interface IDestinationsProps {}

const Destinations: React.FC<IDestinationsProps> = ({}) => {
  return (
    <div>
      <h1>Destinations</h1>
    </div>
  );
};
export default Destinations;
